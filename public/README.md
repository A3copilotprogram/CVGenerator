# Public Assets Directory

This directory contains static assets that are publicly accessible.

## Structure

- **`css/`** - Stylesheets for the application UI
- **`js/`** - Client-side JavaScript files
- **`images/`** - Image assets (logos, icons, sample avatars, etc.)

These assets are served directly to the client and should be optimized for web delivery.
