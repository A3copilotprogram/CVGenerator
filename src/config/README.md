# Configuration Directory

This directory contains application configuration files and settings.

## Purpose

Store configuration for:
- Database connections
- API endpoints
- Environment variables
- Application constants
- Feature flags
- Third-party service credentials (template only, not actual secrets)

Configuration should be environment-aware (development, staging, production).
