# CSS Stylesheets

This directory contains CSS files for styling the Blazor application.

Typical files:
- `app.css` or `site.css` - Global application styles
- `bootstrap/bootstrap.min.css` - Bootstrap framework (if used)
- Custom theme stylesheets

Note: Blazor also supports CSS isolation with `.razor.css` files alongside components.
