# Tests Directory

This directory contains all test files for the CV Generator application.

## Structure

- **`unit/`** - Unit tests for individual functions, classes, and modules
- **`integration/`** - Integration tests for testing component interactions and API endpoints

## Running Tests

Tests should be run before committing code changes to ensure application stability and correctness.
